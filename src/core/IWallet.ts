export enum WalletType {
    NONE = -1,
    BTC = 0,
    EVM = 1,
}

export enum WalletName {
    NONE,
    UNISAT,
    METAMASK,
    WALLETCONNECT,
    OKX,
}

export enum CurrencyName {
    NONE,
    BTC,
    USDT_BSC,
    USDT_ARB,
}

export enum BitcoinNetwork {
    Livenet = 'livenet',
    Testnet = 'testnet',
}